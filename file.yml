- name: demo vars from file
  hosts: localhost
  vars_files:
    test.yml
  tasks:
    - name: PRINT URL
      ansible.builtin.debug:
        msg: URL = {{ URL }}

- name: demo on roles
  hosts: local host
  tasks:
  roles:
    - sample


